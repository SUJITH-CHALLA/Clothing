const fs = require('fs'); const path = require('path'); const dirs = ['track', 'returns', 'contact', 'privacy', 'terms', 'refunds', 'new-drops', 'collections', 'sale']; dirs.forEach(d => { const dirPath = path.join('src/app/(shop)', d); fs.mkdirSync(dirPath, { recursive: true }); fs.writeFileSync(path.join(dirPath, 'page.tsx'), export default function Page() { return <div className='min-h-screen flex items-center justify-center'><h1 className='text-4xl font-bold uppercase'></h1></div>; }); });
